typedef enum {
	THEME_DARK,
	THEME_LIGHT,
	THEME_MODES
} ThemeMode;

typedef enum {
	COLOR_BACKGROUND,
	COLOR_TEXT,
	COLOR_SELECT,
	COLOR_INVALID,
	THEME_COLORS
} ColorEnum;
	
typedef enum {
	THEME_MONO,
	THEME_CHARIZARD,
	THEME_VENUSAUR,
	THEME_BLASTOISE,
	THEME_AERODACTYL,
	THEME_DRAGONITE,
	THEME_MEW,
	THEME_SCYTHER,
	THEMES
} Theme;
